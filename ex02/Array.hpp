#ifndef ARRAY_HPP
# define ARRAY_HPP

template <typename T>
class Array {
	private:
	protected:
	public:
		Array();
		~Array();
		Array(const Array& other);
		Array&	operator=(const Array& other);
};

#endif
